# Vocus 公開文章盤點

日期：2026-07-13  
狀態：`PENDING_USER_ACCEPTANCE`（功能測試通過，待使用者驗收後才發布）

## 已核對事實

- 公開作者頁：`https://vocus.cc/user/@Orikan`。
- 公開文章 API 的作者資料顯示 `articleCount: 36`、`contentCount: 36`。
- 已使用公開 API 逐頁取得 36 篇，且每篇都是 `status == 2` 的公開文章；取得數量與公開計數一致。
- 現行正式官網為 17 篇；完整同步測試站為 36 篇，差額 19 篇。
- 完整同步測試站下載並驗證 94 張原始 Vocus 圖片；文章、RSS、sitemap、llms.txt 均依 36 篇重新產生。
- 全部 36 篇均已配置語意網址、人工撰寫的搜尋摘要與文章主題；舊的 Vocus ID 網址會導向新網址，不會讓既有分享連結失效。

## 不能宣稱的事

目前不能把正式官網的「17 篇」宣稱為 Vocus 所有歷史公開文章。公開計數與逐頁取得結果均為 36 篇，現行正式官網仍少 19 篇。

## 下一步

1. 使用者開啟本機完整測試站，驗收 36 篇的列表、文章頁與圖片呈現。
2. 使用者確認後，才將完整目錄發布到正式官網並公開讀回。
3. 發布後，以 Google Search Console 與 Bing Webmaster Tools 提交 sitemap 並確認收錄狀態；此步驟需要網站擁有者登入既有帳號。

## 本次功能測試證據（去識別化／唯讀）

- 產生指令：`scripts/sync_vocus_catalog.py --creator-id 694518cffd89780001ebc82b --site-root /private/tmp/taixin-full-vocus-preview --write`。
- 靜態檢查：36 篇文章、94 張原圖 hash、36 個 RSS 項目、49 個 sitemap URL、36 個 llms.txt 文章連結均通過。
- 實際瀏覽器（桌機）：文章列表 36 張卡片、CSS 已載入、沒有水平溢出、3 個主題入口。
- 實際瀏覽器（手機 390px）：36 張卡片、內容寬度 390px、沒有水平溢出。
- 歷史文章回放：`你越拜託客戶，客戶越想封鎖` 可開啟，含 4 張 Vocus 原圖、原文來源連結、canonical URL、`BlogPosting` 結構化資料與 CTA。
- 最終 SEO 回放檢查：36 篇／36 個唯一語意網址／94 張原圖／36 個 RSS 項目／49 個 sitemap URL／36 個 llms.txt 連結／36 個 Vocus ID 舊址導向，全部通過。
- 最終實際瀏覽器（桌機）：36 張卡片、正式 CSS 已載入、沒有水平溢出、`client-follow-up-no-reply` 等語意網址可點開。
- 全目錄 release gate：`scripts/verify_vocus_catalog.py --site-root <stage> --expected-public-count 36` 已以本次真實公開清冊通過；檢查原圖 hash、語意網址、description、topic、canonical／schema／來源、RSS、sitemap、llms.txt、robots 與舊址導向。
- 作者可見性驗收：Blog 首頁新增李泰欣照片與自我介紹；每篇文章標題下顯示「本文作者：Orikan 李泰欣」並連回官網關於我，文末另有作者卡。桌機與 390px 手機版皆無水平溢出；首頁與文章的 `Person`／`BlogPosting` schema 也帶有職稱、介紹、照片與 Instagram。
- 測試站資產修正：作者照片使用正式官網既有的 `img/portrait.jpg`。同步產生器現在會把這張既有照片一併寫入任何指定的測試站根目錄，避免測試頁只產生文章而顯示破圖或替代文字；不會生成、替換或修改原始作者照片。
- 破圖修正回放：完整測試站重新由 36 篇公開文章產生後，作者照片 URL 實際讀回為 `http://127.0.0.1:4179/img/portrait.jpg`，`naturalWidth: 918`、`naturalHeight: 1374`、桌機顯示尺寸 `126px`、手機 390px 顯示尺寸 `94px`；桌機與手機均無水平溢出、瀏覽器 console error 為 0。照片 SHA-256 與正式官網 `img/portrait.jpg` 完全一致。
- 人像裁切修正：作者照片維持既有圓形框與原始檔，改為從照片上緣取景（`object-position: center top`），避免直式肖像在正方形圓框中切掉頭頂。實測桌機為 `126px`、390px 手機為 `94px`，兩端皆成功載入、取景位置為 `50% 0%`，且沒有水平溢出。

## 邊界

- 未登入、未修改、未發布 Vocus。
- 未讀取客戶資料、secrets 或私人草稿。
- 未建立自動排程，避免把未確認或非公開內容同步至官網。
- 本次只讀公開 Vocus 文章，沒有在 Vocus 新增、修改或發布內容；也沒有執行正式網站部署。
