# 更新紀錄

日期：2026-07-13

本輪建立個人官網 Blog、文章頁、RSS、sitemap、robots、404、BlogPosting／BreadcrumbList／Person Schema 與站內 CTA；首頁只新增「文章」導覽連結，未改動既有課程內容。

同步方式由示意稿改為「Vocus 已公開且已驗證 → 官網靜態產生器」。以真實發布紀錄 `ORI-SYNC-20260713-VOCUS-6A5439EC` 回放〈德性能力結果金字塔〉：Vocus `status == 2`、公開網址、封存原文與 SHA-256 均經只讀驗證；官網端實際產生文章、列表、RSS、sitemap、文章資料索引、台帳與 artifact passport。先前示意文章已不再出現在官網輸出。

新增可重跑的 `scripts/sync_vocus_article.py` 與 `scripts/verify_vocus_blog_sync.py`。驗證包含 dry-run、實際本機寫入、來源雜湊、SEO／Schema／RSS／sitemap／台帳一致性、`git diff --check`，以及桌面與 390px 手機瀏覽器預覽。Vocus CDN 圖片無法載入時改用官網備援封面，避免破圖。

依使用者指出「官方網站看不到」的回饋，首頁新增正式的「Latest Insights」文章區塊，而不只保留導覽列連結。區塊直接顯示〈德性能力結果金字塔〉的摘要、金句、閱讀按鈕與全部文章入口；桌面與 390px 手機檢查確認文章卡片可見、兩個連結正確、卡片本身無寬度溢位。

已將僅含本次 Blog 的兩個提交推送至 GitHub `main`，GitHub Pages 公開網址已讀回首頁、`/blog/`、文章頁、RSS、sitemap 與 robots。公開手機 390×844 驗收確認文章頁無水平溢位且首頁文章卡片完整可見。另發現 404 的相對復原路徑在深層網址會迴圈，已用獨立提交 `1527ee0` 改為固定首頁與 Blog 路徑，並完成公開讀回。未操作 Vocus、表單、Cloudflare、付款、secrets 或 `l2_current_truth.json`；搜尋引擎收錄提交不在本輪範圍。

端到端測試證據見同層 `verification-summary.md`。本輪已部署並 git push 至 GitHub Pages；沒有 Vocus 發布／修改、表單送出、外部帳號建立、Cloudflare／付款／secrets／`l2_current_truth.json` 變更。

---

日期：2026-07-13

依使用者確認的統一製作標準，本輪 SEO／GEO 改版先完成可操作的本機功能驗收與使用者確認，再以乾淨分支提交 `2033223` 推送 GitHub Pages；公開讀回已完成，狀態為 `RELEASE_READY`。詳細內容、端到端測試證據、未完成風險、外部動作與回滾方式，見同層 `2026-07-13_SEO_GEO功能測試紀錄.md`。

---

日期：2026-07-13

發現已發布 Vocus evidence record 的 immutable archive hash 與官網 36 篇清冊同一 article ID 的來源 hash 不一致。此發現僅標記為 `PROVENANCE_HASH_MISMATCH_NOT_CONTENT_DRIFT_PROVEN`，不推論 Vocus 內容已變更。新增 `scripts/audit_vocus_published_evidence.py`、其去識別化端到端測試，以及單篇同步器的 no-overwrite guard；真實回放得到 `BLOCK_SYNC_NO_OVERWRITE`，同步器未寫入任何官網輸出。同步 SOP 已改為先比對 published evidence／archive／registry，再由人讀回 canonical、決定是否可同步。未操作 Vocus、部署、Git push、Cloudflare、付款、secrets 或客戶資料；詳見 `2026-07-13_Vocus來源追溯防呆與共用Skill建議.md` 與 `2026-07-13_Vocus來源追溯稽核.json`。

同日新增 `scripts/verify_blog_release_scope.py` 與去識別化測試：未來 Blog 發布候選必須在乾淨 worktree，且 diff 只能包含 Blog 允許清單與 evidence audit PASS。以目前混有後台、課程與其他未審核變更的工作目錄回放，工具正確回傳 FAIL；未進行發布、Git push 或任何外部動作。

2026-07-14 以唯讀公開 Vocus API snapshot 追查 hash 差異：已確認公開版完整保留原 archive，但在文末新增發布後 CTA；官網清冊的來源 hash 對應公開版 Lexical 原文。audit 新增 `--public-snapshot` 比對，僅保存關係、字數與 hash 判定，不保存公開文正文。舊 archive evidence 未被修改；仍需建立新的公開版 evidence record 與完成 canonical 讀回，故同步維持 `BLOCK_SYNC_NO_OVERWRITE`。

2026-07-14 使用者明確授權「推上最新版」後，唯讀 Vocus 公開清冊讀回由 36 篇變為 37 篇（新增〈載的是一起買的食材〉），因此本次乾淨發布改以 37 篇、96 張原圖為準。新文建立語意網址 `ability-for-family` 與原文內容相符的搜尋摘要；原文、原圖、Vocus 來源連結均保留。已新增「只允許個人站發布、Vocus canonical 尚待讀回」的明確 audit 決策與去識別化測試；它不允許改 Vocus，也不把 canonical 待確認宣稱為完成。

2026-07-14 新增公開收錄讀回器與去識別化搜尋成效快照驗證器。公開讀回實測 37 篇文章、robots、sitemap、RSS 與 Blog 首頁全數 PASS；Search Console／Bing owner-account readback 與 CTA 第三方分析設定仍明確標為 PENDING，未登入、送件或寫入任何外部平台。

2026-07-14 修正公開讀回器的來源選擇：它原本會拿執行時工作資料夾的本機文章清冊當公開標準，因此舊的 17 篇工作目錄可能把實際已上線的 37 篇網站誤判為 FAIL。現在讀回一律以公開站自己的 `data/blog/articles.json` 為標準，並新增 `--expected-public-count` 與 `--require-local-catalog-match`，只有明確要求時才驗證候選清冊必須與公開站完全一致。去識別化端到端測試涵蓋「本機過期、公開正常」仍正確 PASS；另將遺漏於乾淨候選版的 Vocus 候選輸出隔離與 transition gate 一併補齊。驗證：20 項去識別化測試、37 篇／96 張原圖／RSS 37／sitemap 50 本機檢查，以及公開 37 篇 crawler-facing readback 全數 PASS。未改 Vocus、未建立站長 property、未提交 sitemap、未改 Cloudflare／付款／secrets；可回滾方式為還原本段涉及的 6 個 `scripts/` 檔案提交。

2026-07-14 已將上述安全同步與公開讀回防呆推送至 GitHub `main`（`9faf188`），GitHub Pages workflow `29326826075` 完成成功部署。部署後以 37 篇候選清冊重跑公開讀回：公開 catalogue 37、候選清冊完全一致、37 篇文章 HTTP 200、robots／sitemap／RSS／Blog 首頁均 HTTP 200，並再次驗證 96 張 Vocus 原圖、RSS 37、sitemap 50 全數 PASS。這次推送沒有改動文章正文或圖片、沒有操作 Vocus、表單、Cloudflare、付款、secrets 或 `l2_current_truth.json`。Search Console／Bing 的 owner-account 驗證與 sitemap 送件仍為下一個未完成 Gate；若需回滾，可將 `main` 回復到 `cf88b32`。
