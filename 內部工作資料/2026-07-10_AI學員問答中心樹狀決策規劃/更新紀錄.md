# 更新紀錄

## 2026-07-10

### 新增

1. 建立 `2026-07-10_AI學員問答中心樹狀決策規劃.md`。
2. 依泰欣老師最新需求，將 AI 學員問答中心改規劃為「選擇題 + 決策樹 + 必要自由輸入」的穩定問答流程。
3. 納入 4U 顧問商談地圖的五大核心問題、課程問答、DISC 語氣、異議處理與課程導流邏輯。
4. 補上五大課程、開課資訊、銷售天賦報告、分享圖、Lead Hub 後台保存與五組端到端測試案例。

### 尚未執行

1. 尚未修改 `ai-student-qa.html`。
2. 尚未修改前台聊天流程。
3. 尚未部署 Lead Hub。
4. 尚未推送 `main`。

## 2026-07-10 補充：客戶全問題庫

### 新增

1. 補上 `客戶全問題庫`，把客戶可能直接輸入的問題整理成 Q0-Q24。
2. 每一類問題都寫明「可能問法、判斷路徑、下一步、推薦回覆」。
3. 補足個資、生日、Email、課程適配、課程內容、開課時間金額、產業適合度、效果證據、忙碌效率、價格、主管決策、報名流程、下載分享、技術抱怨、免費內容與直接建議等情境。
4. 明確規定 AI 不可自行猜日期、名額、付款方式、成效數字或未確認保證。

## 2026-07-10 補充：三輪批判

### 新增

1. 補上 `三輪批判與改進`，從使用者體驗、銷售轉換、系統穩定三個角度檢查決策樹。
2. 明確新增三題後的 `confidence_score` 補問機制。
3. 明確規定每次整理說明後要有三個延伸問題按鈕加一個 `我都清楚了` 按鈕。
4. 補上價格異議細分、低壓 CTA、人工接手、重複防呆、Lead Hub 路徑保存與決策樹版本控管。

## 2026-07-10 補充：四按鈕與 DISC 分支重規劃

### 修改

1. 將每次說明後的按鈕規則，由三顆修正為四顆：三個客戶看得懂的延伸問題，加一個 `我都清楚了`。
2. 明確規定前台不可出現內部分類、節點名稱、信心分數、handoff、版本號或工程語。
3. 新增 `DISC 行為模式判斷樹`，用四題銷售情境題判斷 D/I/S/C 初步傾向。
4. 新增 D/I/S/C 對應回話方式與四顆按鈕範例。
5. 新增 `前台文案核對規則`，正式實作前需掃描所有可見文案，避免 AI 味、內部術語與未確認事實出現在頁面。

## 2026-07-10 補充：第二次三輪批判

### 新增

1. 補上 `第二次三輪批判：還沒寫好的決策樹`。
2. 指出目前仍未完整成樹的 12 個部分：清楚後續、報告修正、跳題切換、價格細分、報名前確認、顧問接手、後台名單查詢、課程資訊管理、隱私同意、回訪接續、下載分享失敗、壞情境測試。
3. 新增 `需要補寫的決策樹清單`，作為下一輪轉 JSON 與實作前的檢查表。

### 待確認

泰欣老師確認本決策樹後，再開始網站更新與端到端驗證。

## 2026-07-10 實作完成：決策樹 MVP

### 修改

1. `ai-student-qa.html` 改為決策樹問答介面，並保留自由輸入。
2. 新增產品 / 服務欄位與第四題 DISC 題目。
3. 報告上方固定顯示「下載 9:16 圖片」「分享圖片」「建議學習路徑」三個動作。
4. 修正「企業顧問」被誤判成 LINE 顧問、「價格」在前三題被誤判成開課費用的問題。
5. `lib/state-machine.js` 補上價格、再想想、問別人、信任、效率、表達、名單七大分支與第二層回答。
6. `lib/gemini-student-qa.js` 改成安全包裝器，前端不放 API key，不直接呼叫 Gemini。
7. `data/taixin-sales-brain.json` 移除不適合對外載入的舊 AI 口吻。

### 驗證

1. `node --check lib/state-machine.js`：PASS。
2. `node --check lib/gemini-student-qa.js`：PASS。
3. `verify_decision_tree_10_cases.js`：PASS，10 個不同行業案例都能完成問答、報告、課程路徑與課程內容回答。
4. `verify_links_and_static.js`：PASS。
5. `git diff --check`：PASS。

### 限制

1. 本版採可預測的決策樹，不讓前端自由呼叫 Gemini；Gemini 或其他模型日後只能由後端受控使用。
2. 分享按鈕會優先使用裝置原生分享；裝置不支援時會下載 9:16 PNG。

## 2026-07-10 最終驗證與上線準備

### 修正

1. 每一輪已完成的四顆選項按鈕會鎖定並淡出，只保留最新一組可操作按鈕，避免學生重點到舊題目造成重複問答。
2. 下載圖片改用 `downloadBlob()`，並延後 1.5 秒釋放 Blob URL，避免瀏覽器尚未開始下載就失效。
3. 保留正式 `data/lead-hub-config.json` 的啟用設定與 Cloudflare Worker capture URL，沒有覆寫成測試值。

### 驗證

1. 本機瀏覽器完整走過：基本資料、三題探索、四按鈕分支、`我都清楚了`、銷售天賦報告、三堂學習路徑與 CTA。
2. 實機確認已完成題目的選項全部 disabled，最新節點維持四顆可選按鈕。
3. 手機 390px 與桌面 1440px 均無橫向溢位；報告、課程路徑與三個行動按鈕可正常閱讀與操作。
4. `verify_decision_tree_10_cases.js`：PASS，涵蓋保健、房仲、醫美、企業顧問、教育、汽車、金融保險、健身、SaaS、馬來西亞直銷十個案例。
5. `verify_links_and_static.js`、課程資訊驗證、Lead Hub 安全驗證、JavaScript 語法檢查、`git diff --check` 與 API key 掃描均 PASS。

## 2026-07-10 報告資料與分支校正

### 修正

1. 刪除自行命名、但不在生涯運數資料庫裡的「目標貫通線」。
2. 九宮連線名稱改為對照 `生涯運數計算器/data.js` 的正式資料；`159` 固定顯示為「事必躬親線」。
3. 生涯運數三點天賦改依同一資料庫的「給業務的建議」表白話整理，不再用未經來源確認的標題。
4. 「銷售力不從心，因為從沒遇過李泰欣。」保留為頁首品牌標語，不再作為送給學生的最後一句話；最後一句依學生目前問題選用可實際幫助他的金句。
5. 「怎麼講得更有感」「怎麼講出差異」「怎麼講給主管聽」拆成三條不同回答與不同下一步按鈕。
6. 已回答的按鈕群組改為隱藏，只保留學生實際選擇與最新一組四按鈕，避免畫面出現兩組相似選項。

### 新增驗證

1. 新增 `159` 九宮連線來源驗證，確認報告不再出現「目標貫通線」。
2. 新增三個表達分支回答去重驗證，三顆按鈕必須產生三段不同內容。

## 2026-07-10 決策樹按鈕去重修正

### 問題與修正

1. 發現「怎麼看懂客戶」「怎麼問出真話」「怎麼讓客戶安心」等不同按鈕會被共用的分支摘要覆蓋，學生看到的回答相同。
2. 原因是含有「價格」「工具」等字的樹狀按鈕，會先被全站的課程查詢規則攔截，沒有進入自己的節點。
3. 問答階段已改為先判斷 `TREE_BUTTON_TO_NODE`，再處理開課時間、費用、報名等全站問題。
4. 樹狀內容目前有 21 個第一層獨立節點，每個節點各有 3 個不同的第二層回答，共 84 個可驗證的節點回覆；不宣稱未實作的「一千多句」。

### 驗證規則

1. 自動測試逐一走完 21 個第一層按鈕，第一層回覆不得重覆。
2. 每一個第一層節點都逐一走完 3 個第二層按鈕，63 個第二層回覆不得與同節點的其他選項重覆。
3. 靜態檢查確認前台載入明確節點、保存目前節點，且問答階段的節點判斷優先於全站查詢。

## 2026-07-11 五堂課公開介紹接回決策樹

### 來源核對

1. 重新逐堂核對 `/Users/macminim4/Desktop/Claude cowork/4u-internal顧問商談地圖/index.html` 與 `mindmap.html` 的教育顧問內容。
2. 對外可使用的內容整理為：課程定位、學生會學到的三個方向、適合處理的問題，以及五堂課之間的學習關係。
3. 未確認的成效數字、個案成績、年資與完整付費教材話術不放入學生前台回答。

### 實作

1. 五堂課現在都有完整的公開介紹，不再只有一行利益點：流量磁鐵、成交地圖、直指人心、極致效率、言之有物。
2. 課程介紹分成「五堂課總覽」「指定課程詳細內容」「其餘三堂課」三個節點，學生可以按選項逐段閱讀。
3. 課程內容接入教育顧問網站已核對的核心架構：天地人網、存→記→分→細、第一印象、五大核心問題、MBAF、成交 365、DISC 四型、時間碼與行事曆、智能 AI 成交助手、能量×邏輯×格局、三種角色溝通、30 秒電梯簡報與 YPD 三部曲。
4. 每一段說明仍固定以「以上的說明都清楚嗎？」收尾，並保留三個不同延伸選項加上「我都清楚了」。

### 驗證

1. `verify_decision_tree_10_cases.js`：PASS，10 個不同行業案例完成三題探索、路徑選擇、銷售天賦報告與五堂課內容回答。
2. `verify_gemini_flow_mock.js`：PASS，確認課程介紹包含五堂課與核心架構。
3. `verify_course_offerings.js`：PASS，課程日期、價格、複訓費用與待確認欄位沒有被本次內容改動。
4. `verify_links_and_static.js`：PASS，確認沒有「目標貫通線」、品牌口號金句誤送、HTML 標籤外露或內部工程文字。
5. `git diff --check`：PASS。
