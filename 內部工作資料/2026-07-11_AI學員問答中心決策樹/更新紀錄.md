# 更新紀錄

## 2026-07-11 學生版決策樹 v2

### 新增

1. 建立 `2026-07-11_學生版決策樹_v2.md`，作為學生問答中心的唯一行為規格。
2. 建立 10 位學生、15 個課程節點、7 個疑慮節點的自動驗證。
3. 建立本機瀏覽器端到端測試，涵蓋表單、聊天、報告、下載、桌面與手機畫面。

### 修改

1. DISC 從前置表單移入三題學生本人問答；課程推薦改用另一條客戶情境線。
2. 五堂課改為每堂三個獨立 MBAF 學生問題，並保留 4U 的 DISC 語氣來源。
3. 報告新增「建議補強的點」，分享圖同步顯示。
4. 固定保留「課程介紹」與「我的銷售天賦」入口。
5. 超級銷冠系統資料源更新：台灣 10/16-10/18、NT$31,000；吉隆坡 10/21-10/23、RM4,680；兩場不顯示未確認上課時間。

### 驗證

1. 決策樹 10 位學生：PASS。
2. 15 個課程節點不同回答：PASS。
3. 7 個疑慮節點不同回答：PASS。
4. 課程資料 schema 與公開資料：PASS。
5. 瀏覽器：開始招呼、報告、下載、課程推薦、桌面/手機畫面：PASS。
# 2026-07-11 成交地圖資料驅動化

- 新增 `data/taixin-closing-map.json`，作為泰欣學生版成交地圖的公開內容主檔。
- 新增三題 DISC、十五個五堂課節點、七種學生疑慮與四型回覆的可審核資料。
- `lib/gemini-student-qa.js` 與 `lib/state-machine.js` 改為讀取成交地圖內容，不再只依程式內固定題庫。
- 新增 `verify_closing_map.js`，檢查課程節點、疑慮節點、四型回覆與同層重複。
- 確認台灣超級銷冠系統 `10/16-10/18`、`NT$31,000`；吉隆坡 `10/21-10/23`、`RM4,680`；未確認時間不顯示。
- 端到端測試：10 個學生情境、15 個課程節點、7 個疑慮、桌面與手機、報告下載，均通過。

# 2026-07-11 成交地圖內容完整性複盤

- 使用者指出：推薦順序未直接列在聊天回覆，且課程介紹缺少完整 MBAF。
- 稽核結果：現行資料驅動化只完成部分。十五個課程節點沒有完整 MBAF 欄位與四型完整回答；二十一個疑慮追問答案仍在程式；重新 render 報告會清除底下的建議學習路徑。
- 已新增 `2026-07-11_成交地圖逐節點稽核.md`；狀態標記為 `FAIL_HUMAN_OUTCOME`，待逐句內容補齊與重新驗收前，不可宣稱完整成交地圖。

# 2026-07-11 成交地圖資料庫主檔化

- 新增 `成交地圖資料庫.md`，作為唯一可修改、可審稿的學生問答主檔；內容資料區可產生網站用 JSON。
- 新增 `build_closing_map_database.js`，由 Markdown 產生 `data/taixin-closing-map.json` 與 `成交地圖資料庫_逐句展開.md`。
- 逐句展開版目前包含：15 個課程問題的 MBAF 四段與 D/I/S/C 回覆；7 種疑慮、21 個追問及其 D/I/S/C 回覆；固定開場、課程推薦、課程總覽、疑慮收尾文字。
- 網站改為優先讀取每個課程節點和疑慮追問的資料庫回覆；推薦結果直接在聊天內列出三堂課，底下學習路徑不會因後續課程對話消失。
- 驗證：資料庫與 JSON 一致性、15 MBAF 節點、7 種疑慮、21 個追問、10 位學生情境、課程資訊檢查與 `git diff --check` 均通過；待最終瀏覽器與公開站驗收後上架。

# 2026-07-11 產品介紹口語順序與單堂課 CTA

- 唯一內容來源 `成交地圖資料庫.md` 的 15 個課程節點已改為：第一段 Mini Yes 問句、第二段「你學會之後」的 Benefit、第三段 Advantage、第四段 Feature。
- 每個節點的「我都清楚了」改為 `course-next-step`，不再強迫學生看完五堂課或只回總覽。
- 新增四條完成後下一步：`我要報名課程`、`找教育顧問聊一聊`、`我還有一個疑慮`、`看看其他課程`；狀態機分別接至表單、LINE、疑慮處理與課程總覽。
- 驗證新增：逐一檢查 15 節點的 Mini Yes 必須是問句、四型回覆第一段含 Mini Yes、第二段含 Benefit、以及每一節點都有正確單堂課 CTA。
- 可複用規則已同步回 `TaixinMemoryHub/tools/website-deployment/SKILL.md` 與 `TaixinMemoryHub/references/成交地圖共用母版.md`。
